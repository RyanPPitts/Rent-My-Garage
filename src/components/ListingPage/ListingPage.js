import React from 'react'

function ListingPage() {
    return (
        <div>
            <p>This is the listing page. This is where the user can upload a new listing for rent</p>
        </div>
    )
}

export default ListingPage
